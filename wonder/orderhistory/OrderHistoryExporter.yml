apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-exporter
  namespace: dev-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-exporter
  template:
    metadata:
      labels:
        app: order-exporter
    spec:
      containers:
      - name: exporter
        image: handsomedyman/data-exporter:latest
        resources:
          requests:
            cpu: 100m
            memory: 4G
          limits:
            cpu: 2000m
            memory: 8G
        env:
        - name: DB_HOST
          value: ""
        - name: DB_USER
          value: ""
        - name: DB_PASSWORD
          value: ""
        - name: OUTPUT_DIR
          value: ""
        volumeMounts:
        - name: export-volume
          mountPath: /app/export_results
      volumes:
      - name: export-volume
        emptyDir: {}
